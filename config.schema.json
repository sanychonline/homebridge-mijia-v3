{
  "pluginAlias": "Xiaomi Camera 1080P",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "headerDisplay": "Xiaomi 1080p local camera plugin. Most tuning options have safe defaults and can still be added manually to config.json when debugging.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Plugin Name",
        "type": "string",
        "default": "Xiaomi 1080p"
      },
      "cameras": {
        "title": "Cameras",
        "type": "array",
        "items": {
          "title": "Camera",
          "type": "object",
          "properties": {
            "name": {
              "title": "Name",
              "type": "string",
              "required": true
            },
            "did": {
              "title": "Xiaomi Device ID (DID)",
              "type": "string",
              "description": "The numeric device ID assigned to this camera in the Xiaomi/Mi Home account database. This is not the camera IP address, MAC address, serial number, or device key.",
              "required": true
            },
            "ip": {
              "title": "Local IP",
              "type": "string",
              "placeholder": "192.168.1.50",
              "required": true
            },
            "deviceKey": {
              "title": "Device Key",
              "type": "string",
              "format": "password",
              "required": true
            },
            "model": {
              "title": "Model",
              "type": "string",
              "default": "mijia.camera.v3"
            },
            "hsv": {
              "title": "HomeKit Secure Video",
              "type": "boolean",
              "default": true
            },
            "audio": {
              "title": "Camera Microphone Audio",
              "type": "boolean",
              "default": true
            },
            "twoWayAudio": {
              "title": "Two-Way Audio / Talkback",
              "type": "boolean",
              "default": false,
              "description": "Advertises HomeKit Talk support. Camera speaker transport is still experimental."
            },
            "backgroundMonitoring": {
              "title": "Background Still Image / Motion Monitoring",
              "type": "boolean",
              "default": true,
              "description": "Keeps a lightweight local SUB stream open for still images and motion detection."
            },
            "motionDetection": {
              "title": "Motion Detection",
              "type": "boolean",
              "default": true,
              "description": "Uses native Xiaomi motion events when available and local SUB packet activity as a cloud-free fallback for HomeKit motion/HKSV."
            },
            "maxStreams": {
              "title": "Maximum Concurrent HomeKit Streams",
              "type": "integer",
              "default": 5,
              "minimum": 1
            }
          }
        }
      }
    }
  }
}
